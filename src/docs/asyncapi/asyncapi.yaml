asyncapi: "2.0.0"
info:
    title: Chat WebSocket API
    version: "1.0.0"
    description: WebSocket API for real-time chat features, including sending, editing, pinning, unpinning, and deleting messages.

servers:
  chat-server:
    url: http://localhost:5000
    protocol: ws
channels:
  chat:
    $ref: './channels/chat/chat.yaml'  # Reference to the chat channel file which contains multiple channels
  group:
    $ref: './channels/group/group.yaml'  # Reference to the group channel file, which also contains multiple channels
