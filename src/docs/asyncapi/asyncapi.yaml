asyncapi: "2.0.0"
info:
    title: Chat WebSocket API
    version: "1.0.0"
    description: WebSocket API for real-time chat features, including sending, editing, pinning, unpinning, and deleting messages.

servers:
  chat-server:
    url: http://localhost:5000
    protocol: ws
channels:
  message:
    $ref: './channels/chat/chat.yaml#/message'  # Reference to the chat channel file which contains multiple channels
  edit:
    $ref: './channels/chat/chat.yaml#/edit'  # Reference to the chat channel file which contains multiple channels
  pin:
    $ref: './channels/chat/chat.yaml#/pin'  # Reference to the chat channel file which contains multiple channels
  unpin:
    $ref: './channels/chat/chat.yaml#/unpin'  # Reference to the chat channel file which contains multiple channels
  delete:
    $ref: './channels/chat/chat.yaml#/delete'  # Reference to the chat channel file which contains multiple channels
  create:
    $ref: './channels/group/group.yaml#/create'  # Reference to the group channel file, which also contains multiple channels
  leave:
    $ref: './channels/group/group.yaml#/leave'  # Reference to the group channel file, which also contains multiple channels
