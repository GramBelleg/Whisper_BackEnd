export type UserInfo = {
    email: string;
    userName: string;
    phoneNumber: string;
    password: string;
    name: string;
};

export type DuplicateUserInfo = {
    email?: string;
    userName?: string;
    phoneNumber?: string;
};

